name: Secret
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    env:
      SECRET: ${{ secrets.PASSWORD }}
      MYFRIND: ${{ vars.MYFRIND }}
    steps:
      - run: echo ${MYFRIND}
      - run: echo ${SECRET}
      - run: echo ${PASSWORD:0:1} ${PASSWORD#?}