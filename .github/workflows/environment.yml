name: Secret
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    env:
      SECRET: ${{ secrets.PASSWORD }}
      MYFRIND: ${{ vars.MYFRIND }}
    steps:
      - run: echo ${MYFRIND}
      - run: echo ${SECRET}
      - run: echo ${SECRET:0:1} ${SECRET#?}